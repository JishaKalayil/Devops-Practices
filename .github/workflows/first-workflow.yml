name: First Workflow
on: 
  workflow_dispatch:
    issues:
      types: [opened]
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Step one
        run: echo "Step 1 complete"
        env:
          VARIABLE_NAME: value
      - name: Step two
        run: echo "Step 2 complete"
 